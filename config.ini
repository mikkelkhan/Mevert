[Query]
add_data = INSERT INTO signup (name, username, email, password, city, state, country, pin) VALUES ('$name' , '$username', '$email', '$password', '$city', '$state', '$country', '$pin')
fetch_email = select email, password from signup where email = '$email' and password = '$password'


[MSSQL]
connect = DRIVER={SQL Server};SERVER=Kashif;DATABASE=Mevert

